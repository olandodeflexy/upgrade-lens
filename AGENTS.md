# Project instructions

The user wants testers to run this app locally. Keep the project local-only. Do not create a hosted site, deploy it, or restore public access unless the user explicitly requests hosting again.

Use npm and the existing lockfile. Run `npm test` and `npm run typecheck` for functional changes. For runtime or build configuration changes, also run `npm run build` and check the local server.
